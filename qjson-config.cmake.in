GET_FILENAME_COMPONENT(myDir ${CMAKE_CURRENT_LIST_FILE} PATH)
GET_FILENAME_COMPONENT(rootDir ${myDir}/@relInstallDir@ ABSOLUTE)

SET(@CMAKE_PROJECT_NAME@_LIBRARIES qjson)
SET(@CMAKE_PROJECT_NAME@_INCLUDE_DIR "${rootDir}/@INCLUDE_INSTALL_DIR@")

include(${myDir}/qjson-targets.cmake)
